/**
 * Design System — Root Entry Point
 * Import everything from here:
 *
 *   import { Button, Input, Badge } from "@/design-system"
 *   import { colors, typography }   from "@/design-system"
 */

// Foundations
export * from "./foundations";

// Components
export * from "./components";
